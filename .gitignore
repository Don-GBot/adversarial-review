node_modules/
*.test.json

# Crossfire run artifacts + local audit specs
data/
audit/
*.audit.md

# ...but the gate manifest and the audit spec it references are the published
# contract and MUST ship (a clone needs them for the pre-push gate to work).
!data/
data/*
!data/crossfire-manifest.json
!audit/
audit/*
!audit/crossfire-merged-spec.md
